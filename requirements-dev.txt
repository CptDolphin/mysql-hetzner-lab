# Narzędzia testowe (lokalnie + CI). Instaluj: pip install -r requirements-dev.txt
ansible-core>=2.17
ansible-lint>=24.9
molecule>=24.9
molecule-plugins[docker]>=23.5
pytest-testinfra>=10.1
yamllint>=1.35
