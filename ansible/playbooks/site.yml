---
# Główny playbook. Role dokładane fazami (TASKS.md): common, os_hardening (F3)
# → mysql, proxysql (F4) → backup (F5/6) → caddy, docker_app (F7).
# Każda rola = osobny PR + scenariusz Molecule (converge + idempotencja + Testinfra).
- name: MySQL lab — konfiguracja hosta
  hosts: mysql_lab
  become: true
  gather_facts: true
  roles:
    - common
