---
# Inwentarz — BEZ sekretów i realnych IP w repo.
# Publiczny IP wstrzykiwany z output Terraform / CI (--extra-vars) albo lokalnie przez group_vars (gitignored).
all:
  children:
    mysql_lab:
      hosts:
        db-01:
          # ansible_host: <PUBLIC_IP>   # z Terraform output / CI — nie commitować
          ansible_user: deploy
          ansible_python_interpreter: /usr/bin/python3
